"use client";
import Link from "next/link"; import { usePathname, useRouter } from "next/navigation"; import { History, LayoutDashboard, LogOut, Menu, UserCircle, X } from "lucide-react"; import { useEffect, useState } from "react"; import { createClient } from "@/lib/supabase/client";
export default function Sidebar() { const path = usePathname(); const router = useRouter(); const [open, setOpen] = useState(false); const [email, setEmail] = useState(""); useEffect(() => { createClient().auth.getUser().then(({ data }) => setEmail(data.user?.email ?? "")); }, []); async function logout() { await createClient().auth.signOut(); router.push("/login"); router.refresh(); } const nav = [{ href: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { href: "/dashboard/history", label: "History", icon: History }, { href: "/profile", label: "Profile", icon: UserCircle }]; const content = <div className="flex h-full flex-col bg-slate-950 p-5 text-white"><div className="flex items-center justify-between"><Link href="/" className="text-xl font-bold">Blogify <span className="text-violet-400">AI</span></Link><button className="lg:hidden" onClick={() => setOpen(false)}><X className="h-5 w-5" /></button></div><div className="mt-8 rounded-xl bg-slate-900 px-3 py-3"><p className="truncate text-sm font-medium">{email || "Signed in"}</p><p className="mt-1 text-xs text-slate-500">Your workspace</p></div><nav className="mt-8 space-y-2">{nav.map(({ href, label, icon: Icon }) => <Link key={href} onClick={() => setOpen(false)} href={href} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium ${path === href || (href !== "/dashboard" && path.startsWith(href)) ? "bg-violet-600 text-white" : "text-slate-400 hover:bg-slate-800 hover:text-white"}`}><Icon className="h-5 w-5" />{label}</Link>)}</nav><button onClick={logout} className="mt-auto flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium text-slate-400 hover:bg-slate-800 hover:text-white"><LogOut className="h-5 w-5" />Logout</button></div>; return <><button onClick={() => setOpen(true)} className="fixed left-4 top-4 z-30 rounded-xl bg-slate-950 p-3 text-white shadow-lg lg:hidden"><Menu className="h-5 w-5" /></button><aside className="fixed inset-y-0 left-0 z-20 hidden w-64 lg:block">{content}</aside>{open && <div className="fixed inset-0 z-40 lg:hidden"><button className="absolute inset-0 bg-slate-950/50" onClick={() => setOpen(false)} aria-label="Close menu" /><aside className="relative h-full w-72">{content}</aside></div>}</>; }
